[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "book-agent"
version = "0.1.0"
description = "AI reading companion agent"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
